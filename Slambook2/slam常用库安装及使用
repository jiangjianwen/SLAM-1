#　使用环境Ubuntu18.04

cmake 安装
	1.首先安装gcc与g++编译器
		sudo apt-get install build-essential
		sudo apt-get install gcc 
		sudo apt-get install g++
	2.下载cmake软件包
		https://cmake.org/download/
		下载对应的linux版本 如:cmake-3.16.2.tar.gz
		也可以使用如下命令直接下载 wget https://cmake.org/files/v3.16/cmake-3.16.2.tar.gz
	3.解压并进入当前目录
		tar -zxvf cmake-3.16.2.tar.gz
		cd cmake-3.16.2
	4.编译源码
		sudo ./bootstrap
	5.开始构建
		sudo make
	6.等待完成以后进行安装
		sudo make install 
	7.检查版本信息查看是否安装成功
		cmake --version

Eigen库
	安装:
		sudo apt-get install libeigen3-dev
	使用:
		在CmakeLists中加入: 

		include_directories("/usr/include/eigen3")
		
		注:Eigen库只有头文件不需要链接
Sophus库
	安装:
		注:如使用最新版本请到github自行下载
		git clone https://github.com/gaoxiang12/slambook2
		在3dpart中找到Sophus库
		cd Sophus
		mkdir build
		cd build
		cmake ..
		make
		sudo make install
	使用:
		在CmakeLists中加入:

		find_package(Sophus REQUIRED)
		include_directories(${Sophus_INCLUDE_DIRS})
		target_link_libraries(execute_file_name ${Sophus_LIBRARIES})

Ceres库
	安装:
		1.首先安装所需要的依赖
			sudo apt-get install liblapack-dev libsuitesparse-dev libcxsparse3 libgflags-dev libgoogle-glog-dev libgtest-dev
		2.进入已经下载好的Ceres目录
			cd Ceres
			mkdir build
			cmake ..
			make
			sudo make install
		使用:
			在CmakeLists中加入:

			find_package( Ceres REQUIRED)
			include_directories( ${CERES_INCLUDE_DIRS} )
			target_link_libraries(execute_file_name  ${CERES_LIBRARIES})
OpenCV库
	安装:
		1.从OpenCV官网下载所需要的版本 推荐3.0以上的版本
		2.安装OpenCV的依赖项
			sudo apt-get install build-essential libgtk2.0-dev libvtk5-dev libjpeg-dev libtiff4-dev libjasper-dev libopenexr-dev libtbb-dev
		3.编译软件包
			cd OpenCV
			mkdir build
			cmake ..
			make -j4
			sudo make install
	使用:
		在CmakeLists中加入:

		find_package( OpenCV REQUIRED )
		include_directories(${OpenCV_INCLUDE_DIRS})		
		target_link_libraries(execute_file_name ${OpenCV_LIBS})
















